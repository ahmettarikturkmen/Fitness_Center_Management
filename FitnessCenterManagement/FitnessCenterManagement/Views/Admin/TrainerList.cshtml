@model IEnumerable<FitnessCenterManagement.Models.Trainer>

@{
    ViewData["Title"] = "Antrenör Listesi";
}

<div class="container py-5">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="fw-bold text-white m-0">Antrenör Kadrosu</h1>
            <p class="text-secondary m-0 small">Sistemdeki eğitmenleri yönetin.</p>
        </div>
        <a asp-action="CreateTrainer" class="btn btn-success d-flex align-items-center gap-2">
            <span class="material-symbols-outlined">add</span> Yeni Antrenör Ekle
        </a>
    </div>

    <div class="card border-0 shadow-lg" style="background-color: #1c2721; border: 1px solid #283930 !important;">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0 align-middle" style="color: white;">

                    <thead style="border-bottom: 2px solid #283930;">
                        <tr>
                            <th class="py-3 ps-4" style="color: var(--neon-green);">AD SOYAD</th>
                            <th class="py-3" style="color: var(--neon-green);">UZMANLIK</th>
                            <th class="py-3" style="color: var(--neon-green);">ÇALIŞTIĞI SALON</th>
                            <th class="py-3 text-end pe-4" style="color: var(--neon-green);">İŞLEMLER</th>
                        </tr>
                    </thead>

                    <tbody>
                        @if (!Model.Any())
                        {
                            <tr>
                                <td colspan="4" class="text-center py-5 text-secondary">
                                    <span class="material-symbols-outlined fs-1 d-block mb-3">sentiment_dissatisfied</span>
                                    Henüz kayıtlı antrenör yok.
                                </td>
                            </tr>
                        }
                        else
                        {
                            foreach (var item in Model)
                            {
                                <tr style="border-bottom: 1px solid #283930;">
                                    <td class="ps-4 fw-bold">@item.FullName</td>
                                    <td>
                                        <span class="badge bg-info text-dark">@item.Specialty</span>
                                    </td>
                                    <td class="text-secondary">
                                        @if (item.Gym != null)
                                        {
                                            <span class="d-flex align-items-center gap-2">
                                                <span class="material-symbols-outlined fs-6">apartment</span>
                                                @item.Gym.Name
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td class="text-end pe-4">
                                        <div class="d-flex justify-content-end gap-2">
                                            <a asp-action="EditTrainer" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Düzenle</a>

                                            <form asp-action="DeleteTrainer" asp-route-id="@item.Id" method="post" onsubmit="return confirm('Bu eğitmeni silmek istediğinize emin misiniz?');" style="display:inline;">
                                                <button type="submit" class="btn btn-danger btn-sm">Sil</button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="mt-4">
        <a asp-action="Index" class="btn btn-secondary d-inline-flex align-items-center gap-2">
            <span class="material-symbols-outlined">arrow_back</span> Panele Dön
        </a>
    </div>

</div>